pipeline {
    agent any
    
    parameters {
        booleanParam(name: "BUILD", defaultValue: true, description: "DOCKER_IMAGE_BUILD")
        booleanParam(name: "TEST", defaultValue: true, description: "RUN_TEST")
        booleanParam(name: "PUSH", defaultValue: true, description: "DOCKER_IMAGE_PUSH")

        // SETTINGS
        string(name: "ENV_FILE_PATH", defaultValue: "exchange-rate/.env", description:"ENV_FILE_PATH")

        // CI
        string(name: "IMANGE_NAME", defaultValue: 'exchange-rate', description: "DOCKER_IMAGE_NAME")
        string(name: "DOCKER_TAG", defaultValue: '0.3.0', description: "DOCKER_TAG")
        
        // CD

    }

    environment {
        DOCKER_HUB_URL = "hub.1ife.kr"
    }

    stages {
        stage('🛠 Build Docker Image 🛠') {
            when {
                expression {
                    return params.BUILD
                }
            }
            steps {
                // env.WORKSPACE는 jenkins가 설정해 준 환경 값
                // /var/jenkins_home/wrkspace/test
                dir("${env.WORKSPACE}") {
                    sh "docker build -f backend/dockerfile -t ${IMANGE_NAME}:${DOCKER_TAG} ./backend"
                }
            }
            post {
                success {
                    echo "😄 Docker Build success 😄"
                }
            }
        }

        stage('📖 Run test code 📖') {
            when { 
                expression { 
                    return params.TEST 
                } 
            }
            steps {
                sh "printenv"
                sh "export"
                sh ("docker run --env-file=${env.ENV_PATH}/${params.ENV_FILE_PATH}\
                --rm ${params.IMANGE_NAME}:${params.DOCKER_TAG} python manage.py test")
            }
            post {
                success {
                    echo "😄 Test success 😄"
                }
            }
        }

        // stage('📨') {
        //     steps {
        //         echo "Push Docker Image to ECR"
        //     }
        // }
    }
}